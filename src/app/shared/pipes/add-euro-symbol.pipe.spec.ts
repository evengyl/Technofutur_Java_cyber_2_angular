import { AddEuroSymbolPipe } from './add-euro-symbol.pipe';

describe('AddEuroSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new AddEuroSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
